{
  "name": "corporate-majdoor-janta-party",
  "version": "1.0.0",
  "description": "Corporate Majdoor Janta Party — official satirical portal",
  "private": true,
  "license": "MIT",
  "scripts": {
    "build": "esbuild script.js --bundle --format=esm --outfile=bundle.js --minify",
    "dev": "esbuild script.js --bundle --format=esm --outfile=bundle.js --watch"
  },
  "dependencies": {
    "ogl": "^1.0.11"
  },
  "devDependencies": {
    "esbuild": "^0.25.0"
  }
}
